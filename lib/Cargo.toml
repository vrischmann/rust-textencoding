[package]
name = "textencoding"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "hex"
harness = false

[dependencies]
thiserror = "1"

[dev-dependencies]
claim = "0.5"
criterion = { version = "0.5", features = ["html_reports"] }
